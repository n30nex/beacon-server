# Copy to a root-owned candidate file and replace both placeholders with the
# manifest digests reported by GitHub Actions. Mutable tags are rejected.
BEACON_API_IMAGE=ghcr.io/n30nex/beacon-server@sha256:<64-hex-manifest-digest>
BEACON_WEB_IMAGE=ghcr.io/n30nex/beacon-web@sha256:<64-hex-manifest-digest>
